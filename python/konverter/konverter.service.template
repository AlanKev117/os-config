[Unit]
Description=Bluetooth monitor service
After=multi-user.target

[Service]
Type=simple

ExecStart={SERVICE_DIR}/{ENV_DIR}/bin/uvicorn main:app --host 0.0.0.0 --port 8000
WorkingDirectory={SERVICE_DIR}
Restart=on-failure
User=root

[Install]
WantedBy=multi-user.target
